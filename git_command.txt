# Git version
git --version

# Configure Git
git config --global user.name "bernardlkb"
git config --global user.email "bernardlkb@ukm.edu.my"

# Initialize Git
git init

# Check the status of the Git:
git status